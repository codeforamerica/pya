<% content_for :html_class, "state-file-html-bg--" %>
<% content_for :body_class, "state-file us-state--" %>
<% content_for :page_title, t("views.pages.home.headline") %>

<main id="maincontent">
    <div class="warning">
      <%= t("views.pages.home.closed_warning") %>
    </div>
  <div class="home-container">
    <div class="home-content">
      <h1 class="h1 strong"><%= t("views.pages.home.headline") %></h1>
        <p><%= t("views.pages.home.closed_forever_subheader_html") %></p>
        <%= link_to t("views.pages.home.download_your_return"), year_select_path, class: "button button--primary button--wide" %>
    </div>

    <%= image_tag("welcome.svg", class: "home-image", alt: "") %>
  </div>
</main>
