{
  "scripts": {
    "postdeploy": "bundle exec rails db:migrate && bundle exec rails db:seed && bundle exec rails assets:precompile"
  },
  "environments": {
    "review": {
      "env": {
        "REVIEW_APP": "true"
      },
      "addons": ["heroku-postgresql"]
    }
  }
}